resolver: lts-7.0

packages:
- '.'

extra-deps: []

flags: {}

extra-package-dbs: []

docker:
  enable: true
  run-args: ["--net=bridge", "--publish=9160:9160"]

image:
  container:
    name: game
    base: fpco/haskell-scratch:integer-gmp
    entrypoints:
      - stormcards
