version: "2"
services:
  static:
    build:
      context: "client"
    ports:
      - "8000:80"
      - "443:443"
    volumes:
      - ./client/static/build:/usr/share/nginx/html
    restart: always
  redis:
    build:
      context: "redis"
    restart: always
  game:
    image: "game-stormcards"
    ports:
      - "9160:9160"
    links:
      - redis
    restart: always
