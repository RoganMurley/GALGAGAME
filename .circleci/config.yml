version: 2
jobs:
  build:
    docker:
      - image: fpco/stack-build
    steps:
      - checkout
      - run:
          name: Server tests
          command: cd server; stack test
      - run:
          name: Client build
          command: cd client; gulp build
